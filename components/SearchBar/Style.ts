import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  searchBoxContainer: {
    width: '95%',
    paddingLeft: 20,
    marginTop: 10,
  },
  searchBar: {
    borderRadius: 14,
  },
  rightContainer: {
    width: 40,
  },
});
